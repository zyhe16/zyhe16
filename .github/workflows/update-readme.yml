name: Update README with Daily Content

on:
  schedule:
    # Primary daily update and a same-day fallback, both in UTC.
    - cron: '17 0 * * *'
    - cron: '47 12 * * *'
  push:
    branches:
      - main
  workflow_dispatch:  # Allows manual trigger

jobs:
  update-readme:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Check whether today's content is already present
        id: freshness
        run: |
          if python readme_freshness.py; then
            echo "needs_update=false" >> "$GITHUB_OUTPUT"
          else
            echo "needs_update=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Set up Python
        if: steps.freshness.outputs.needs_update == 'true'
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        if: steps.freshness.outputs.needs_update == 'true'
        run: pip install -r requirements.txt

      - name: Run update script
        if: steps.freshness.outputs.needs_update == 'true'
        run: python update_readme.py

      - name: Commit and push changes
        if: steps.freshness.outputs.needs_update == 'true'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add README.md
          git diff --quiet && git diff --staged --quiet || (git commit -m "Auto-update: Daily content refresh" && git push)

  generate-snake:
    if: github.event_name != 'schedule' || github.event.schedule != '47 12 * * *'
    permissions:
      contents: write
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: generate github-contribution-grid-snake.svg
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: Ate329
          outputs: |
            dist/github-contribution-grid-snake.svg
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark

      - name: push snake.svg to the output branch
        uses: crazy-max/ghaction-github-pages@v3.1.0
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
